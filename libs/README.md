# Shared Libraries

This directory contains shared models and utilities used across the TCG Exchange platform.

## Protocol Buffers

We use Protocol Buffers for cross-language model definitions.

### Setup

1. Install protoc and plugins:

```bash
npm run install:proto
